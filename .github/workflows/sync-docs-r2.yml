# Syncs optimized corpus to R2 buckets for Cloudflare AI Search indexing.
# Uses output/corpus/ (built by tools/build-corpus.py) instead of raw docs.
# Each job only runs when its respective corpus directory changes.
#
# Required GitHub secrets:
#   R2_ACCOUNT_ID       - Cloudflare account ID
#   R2_ACCESS_KEY_ID    - R2 API token access key
#   R2_SECRET_ACCESS_KEY - R2 API token secret key

name: Sync corpus to R2

on:
  push:
    branches: [main]
    paths:
      - "output/corpus/**"
      - "output/pre-release/corpus/**"
  workflow_dispatch:
    inputs:
      branch:
        description: "Which branch to sync (stable, pre-release, both)"
        required: true
        default: "both"
        type: choice
        options:
          - stable
          - pre-release
          - both

jobs:
  sync-stable:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' && (github.event.inputs.branch == 'stable' || github.event.inputs.branch == 'both') ||
      github.event_name == 'push' && contains(join(github.event.commits.*.modified, ','), 'output/corpus/')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync stable corpus to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
        run: |
          aws s3 sync output/corpus/ s3://hydex-corpus-stable/ \
            --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com \
            --delete \
            --size-only \
            --exclude ".gitkeep"

  sync-pre-release:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' && (github.event.inputs.branch == 'pre-release' || github.event.inputs.branch == 'both') ||
      github.event_name == 'push' && contains(join(github.event.commits.*.modified, ','), 'output/pre-release/corpus/')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync pre-release corpus to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
        run: |
          aws s3 sync output/pre-release/corpus/ s3://hydex-corpus-pre/ \
            --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com \
            --delete \
            --size-only \
            --exclude ".gitkeep"
