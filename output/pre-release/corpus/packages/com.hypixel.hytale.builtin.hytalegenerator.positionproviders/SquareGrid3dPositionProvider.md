# SquareGrid3dPositionProvider

Type: class | Package: com.hypixel.hytale.builtin.hytalegenerator.positionproviders | Extends: PositionProvider

public class SquareGrid3dPositionProvider extends PositionProvider

Generates positions on a regular 3D square grid (unit spacing). Iterates all integer positions within the provided bounds.
