# StreamManager.StreamRegistration

Type: record | Package: com.hypixel.hytale.server.core.io.stream

public private record StreamManager.StreamRegistration

Record pairing a `StreamHandlerFactory` with a `QuicStreamPriority`. Fields: `factory`, `priority`.

## Relationships

- Inner type of StreamManager

Also in this package: PendingStreamHandler, StreamHandlerFactory, StreamManager
