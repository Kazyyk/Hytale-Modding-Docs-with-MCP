# VoiceCommand.VoiceStatusCommand

Type: class | Package: com.hypixel.hytale.server.core.modules.voice.commands | Extends: AbstractCommand

public class VoiceCommand.VoiceStatusCommand extends AbstractCommand

Subcommand `/voice status` that displays current voice configuration (enabled, distances, muted count).

## See Also

- VoiceCommand

Also in this package: VoiceCommand, VoiceEnabledCommand, VoiceFullVolumeDistanceCommand, VoiceMaxDistanceCommand, VoiceMuteCommand, VoiceMutedListCommand, VoiceUnmuteCommand

Complete API:
  protected CompletableFuture<Void> execute(CommandContext context)
