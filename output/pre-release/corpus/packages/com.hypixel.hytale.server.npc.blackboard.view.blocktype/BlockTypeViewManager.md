# BlockTypeViewManager

Type: class | Package: com.hypixel.hytale.server.npc.blackboard.view.blocktype

public class BlockTypeViewManager

Manages `BlockTypeView` instances for NPC blackboard queries, handling creation and lifecycle of block type views.

Also in this package: BlockPositionEntryGenerator, BlockTypeView, FoundBlockConsumer

Complete API:
  protected BlockTypeView createView(long index, Blackboard blackboard)
  protected boolean shouldCleanup(BlockTypeView view)

Fields:
private final BlockPositionEntryGenerator generator
