# SpawnMarkersCommand

Type: class | Package: com.hypixel.hytale.server.spawning.commands

public class SpawnMarkersCommand

Subcommand collection for managing spawn markers. Provides `enable`, `disable` (toggles marker-based spawning per world), and `add` (places a spawn marker entity at the player's position with optional flip).
