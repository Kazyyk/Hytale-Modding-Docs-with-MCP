# SpawnSuppressionCommand

Type: class | Package: com.hypixel.hytale.server.spawning.commands

public class SpawnSuppressionCommand

Subcommand collection for spawn suppression zones. Provides `add` (places a suppression entity), `dump` (logs suppression data for current world), and `dumpall` (logs for all worlds).

Also in this package: Add, Add, Add, DisableCommand, DisableCommand, Dump, DumpAll, EnableCommand, EnableCommand, ManualTrigger, SpawnBeaconsCommand, SpawnCommand, SpawnMarkersCommand, SpawnPopulateCommand, SpawnStatsCommand

Complete API:
  private static String dumpWorld(World world)

Fields:
private static final AssetArgumentType<SpawnSuppression,?> SPAWN_SUPPRESSION_ASSET_TYPE
